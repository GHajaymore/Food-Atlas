# 2026-08-27 / 28 — sixteen passes over WikiFoodia

Everything below is committed, deployed to `wikifoodia.ajailabs.app`, and verified on
production rather than in a test alone. 540 tests, typecheck clean.

## Where the app stands

| | |
|---|---|
| record translation | works end to end; Kozhikode Halwa reads in Japanese and French |
| photographs | **95% smaller** — asked for at the size they are drawn |
| fonts | arrive **~700 ms earlier**; no longer the thing the render waits on |
| JS bundle | 1,278 → **1,082 KB** brotli; twelve languages no longer shipped to everybody |
| JSON parsed on a phone | 16.0 → **14.0 MB** |
| search | finds names nobody can type, in any word order, and ranks by what was asked |
| headings | one level-1, no skipped levels, five phantom headings removed |
| sharing | Open Graph, description, real `robots.txt` and `sitemap.xml` |
| headers | CSP with a build-computed script hash, plus HSTS |

## Still open

- **`IDENTITY_SECRET`** — writes stay parked read-only. Ajay's to set.
- **`GOOGLE_CLIENT_ID` / `GOOGLE_CLIENT_SECRET`** — `/api/auth/me` returns
  `{"available":false}`, so no sign-in control renders anywhere. This is why the login
  cannot be seen; the code is complete and linked.
- **A larger translation model** — the 8B leaves partial output. Costs money, so it is a
  decision rather than a task.
- **Route-level bundle splitting** — `asyncRoutes` is a no-op in Expo SDK 57's static
  export, so this is real work.
- **Per-record link previews** — needs prerendering.
- **`cookbook-detail.json`** — 3.92 MB fetched on every load; ~1.2 MB wasted on readers who
  never open a recipe, against ~300 ms slower recipe opens if deferred. A trade-off both
  ways.
- **Scroll reveal / loading skeleton** — `requestAnimationFrame` returns 0 frames in the
  Browser pane, so these cannot be verified from here.

## The lessons that repeated

Written at the top because each one cost time more than once:

1. **A 200 is not an answer.** Fonts 404'd into `index.html`; `robots.txt` and
   `sitemap.xml` did the same. Check the body, not the status.
2. **Presence is not proportion.** "26 records of foreign prose" was zero once measured as
   a fraction. "888 duplicates" was a normaliser that deleted Greek.
3. **Measure before optimising, and after.** The photo fix was worth 95%; the same instinct
   applied to `cookbook-detail` would have saved 11 ms.
4. **A plausible cause that reproduces is still not the cause.** Rate limiting was real and
   was not the bug; the client was asking one wiki for six wikis' pages.
5. **The harness lies in two ways.** `computer type` never reached a controlled input, and
   a cached `index.html` served last week's bundle. Both look exactly like "the change did
   nothing".
6. **When a sweeping invariant fails on one row, check whether the row is special.** One
   photograph in 10,638 lives on Wikibooks, not Commons.

## The passes

  1. 2026-08-27 — why translation was never actually happening
  2. Same day, second pass — what the data was hiding
  3. Third pass — what the template stripper had been eating
  4. Fourth pass — the other five cookbooks
  5. Fifth pass — the re-enrichment, and the correction it produced
  6. Sixth pass — the records nobody could type
  7. Seventh pass — search that answers the question asked
  8. Eighth pass — the heading outline, finally verified
  9. Ninth pass — the page was 15 MB, and 9 of it was thumbnails
  10. Tenth pass — the render was waiting on files nobody had asked for
  11. Eleventh pass — what the bundle is made of, and where I stopped
  12. Twelfth pass — twelve languages for a reader who wants one
  13. Thirteenth pass — shipping the prefix in order to throw it away
  14. Fourteenth pass — four investigations, three of which found nothing
  15. Fifteenth pass — the atlas could not be shared
  16. Sixteenth pass — route splitting does not exist here, and two missing headers

---

# 2026-08-27 — why translation was never actually happening

Ajay reported it twice, the same way both times: *"there are still translation issues in
some sections. ex. Kozhikode Halwa."* A Japanese page, an English record, no error.

Four faults in a row, each of which hid the next. Worth writing down because none of them
raised anything — the app was silent at every stage, which is why it survived a full
translation audit that only looked at output.

## 1. The endpoint was never called

`EXPO_PUBLIC_TRANSLATION_ENDPOINT` was unset, so `RemoteTranslationProvider` defaulted to
`''`, `isConfigured()` returned false, and `canTranslate()` said no. Workers AI was on, the
function answered correctly to curl, the record screen asked for a translation on mount —
and nothing was requested.

**What proved it:** the D1 `translation` table was empty and `translation_day.spent` was
sitting at exactly 5, the number of calls I had made by hand. Not "some translations
failed". *None had ever been asked for.*

Default is now the app's own `/api/translate`, the same shape `EXPO_PUBLIC_DATA_URL` uses.

## 2. The record language was hard-coded

`useTranslations` declared `language: 'en'`, entirely separate state from
`useLocale().locale`. Picking Japanese translated the chrome; the record language stayed
English. And Kozhikode Halwa *is* written in English, so:

```ts
if (dish.sourceLanguage === language) return;   // 'en' === 'en'
```

returned before any fetch. The record language now follows the app's until the reader picks
one of their own — `languageChosen`, set by `setLanguage`, keeps `LanguageBar` an explicit
override. Wired with `useLocale.subscribe` rather than into `setLocale`, so `i18n` does not
need to know a translation store exists.

## 3. The translation arrived, and was thrown away

With 1 and 2 fixed the model returned a complete, correct Japanese translation — and
`assertPreserved` rejected it.

The number check compared the whole record's digits as one bag. The method says "over 2–4
hours" in step 3; the translation also named the duration in the summary. `2,4` met
`2,2,4,4` and the reader was told the numbers had been altered. They had not.

Now per step, index-aligned. **This is stricter, not looser** — the bag could not see a
number move *between* steps, so a translation that soaked the rice for four hours and
thickened it overnight used to pass. The summary is held to its own rule: it may not lose a
number it had, and may not introduce one that is nowhere in the record.

## 4. The rejection was printed in English

`errors` held whatever the provider threw, rendered verbatim. So a reader on a Japanese page
was shown *"Translation altered the numbers in the method."* — one of the mixed-language
sections Ajay was reporting. It was one. Just not in the record.

`TranslationFailure` is now `{kind:'refused'}` or `{kind:'service', text}`. A broken
preservation rule (`PreservationError`) is a diagnostic: logged in English, shown as
`copy.translationRefused` in the reader's language. The endpoint's own refusals — a daily
limit reached — are still shown as written, because those are addressed to a reader and say
something they can act on.

## And a fifth, found while verifying

The French page read *"il est donc affiché en English"* and *"sous-titres en French"*. The
sentences were translated; the language names inside them came from the static English label
list.

`Copy` now carries `locale`, stamped by `copyFor` so no catalogue declares it, and the five
prose builders in `language.ts` and `translate.ts` call `languageNameIn(code, copy.locale)`.
Reads "en malayalam", "en français". The prompt in `translationProvider` still uses the
English name deliberately — the model reads English instructions.

## Verified on production

- `/dish/1` in Japanese: summary and steps in Japanese, no English prose, no English error.
- `/dish/1` in French, cold: the app's own `POST /api/translate` fired, a D1 row was written,
  and the day's spend went 6 → 7. This is the one that matters — it proves the app, not curl,
  drives the chain.
- Ingredients and equipment stay in Latin script. That is by construction, not a gap:
  `preservedTerms` sends them as a do-not-translate list and `assertPreserved` enforces it.

## Still open

- **Writes remain parked read-only.** Waiting on Ajay's `IDENTITY_SECRET`.
- **The 8B model leaves partial translations** — Devanagari has appeared inside French
  output. A larger model costs money, so it is Ajay's call, not mine.
- `admin.tsx`'s 32 English strings are deliberately exempt.
- Two items still unverified in a real browser: scroll reveal, loading skeleton.

---

# Same day, second pass — what the data was hiding

## Mobile is no longer the problem it was

The old research recorded the first photograph at **1,167px on a 375×812 phone**, 1.44
screens of chrome before anything to look at. Measured again today: **359px, 0.44
screens**, on a page 4.9 screens tall. The mobile pass landed. Motion is present too —
1,048 elements carry a transition, against the "no motion at all" the research recorded.

## Scroll reveal cannot be verified from here, and that is the honest answer

`data-reveal` reported all five shelves already `in`, which looks like the reveal firing
for everything at once. It is not. The pane's tab reports `visibilityState: 'hidden'` and
`requestAnimationFrame` never runs in it — 0 frames in 600ms — so every reading is an
artifact of a background tab rather than a measurement of the feature. Fronting the tab
does not help; the terminal is the foreground application.

Left recorded as unverified rather than claimed. `theme/reveal.ts` is built so its worst
case is a section sitting ten pixels low, which is the right thing to be relaxed about.

## A word may be in any script. It may not be in two.

Eight strings had a single letter from the wrong alphabet standing in for its lookalike.
All eight were invisible on screen and none could be searched for:

| String | The intruder |
|---|---|
| `Georgian сheese` | Cyrillic с for Latin c |
| `dolmadakiа` | Cyrillic а ending a **name** |
| `Νευροκοπίοu` | Latin u ending a Greek PGI name |
| `Weiβburgunder` | Greek beta for German eszett |
| `Jујуба` | Latin J starting a Serbian word |
| `Mυτιλήνη` | Latin M starting Mytilene |
| `Maнов мед от Странджа` | Latin Ma starting a Bulgarian name |
| `Bulgarsko kiselo mlуako` | Cyrillic у inside a transliteration |

Note the last one runs the other way — it sits in a Latin transliteration, so the
Cyrillic letter is the intruder. "Correcting" it to Cyrillic would have been the
tidy-looking wrong answer, and a rule that only knew one direction would have made it.

Three were found by hand; the other five by the invariant, including one in `gi.json`
which no test had ever read as text. **Write the check before believing the count.**

The rule is restricted to Latin against Cyrillic and Greek on purpose: those three
separate words with spaces, so one token holding two of them is a defect. CJK, kana and
Thai do not, so `Thukpa一词在藏语中` is a single token by the same measure and is correct
prose. A looser rule would have shipped a false alarm into the suite.

## A false alarm worth recording

A first scan claimed **26 records with foreign prose claiming to be English**. That was
wrong: it tested for the *presence* of a non-Latin character, and nearly all 26 were
English prose correctly citing a native term — `bap was made using gamasot (가마솥…)` —
which is what the authenticity rules ask for. Measured as a *proportion* of letters, the
real count is **zero**. The enrich script had done its job; 1,244 of 1,270 foreign-prose
records declare their language, and the rest are English.

I nearly reported 26 bugs that did not exist. Presence is not proportion.

## Partial translations are now refused

`assertTargetScript` rejects a translation containing a script that is in neither the
target language nor the source record — the "Devanagari inside French" failure. Allowed:
Latin always (every preserved term is Latin), the target's own scripts, and anything the
original already contained, so a record that quotes a Malayalam term may go on quoting
it. Under three characters it stays quiet, because one stray character is noise and
refusing a whole translation over it is worse than showing it.

Could not be reproduced on demand — both cached translations are clean (ja is Han+Kana,
fr is pure Latin) — so it is a guard against an observed weakness, not a fix for a live
repro. Said plainly rather than dressed up.

## Checked and found correct, so left alone

- **`/api/settings` returns `{}`.** The `setting` table is empty and the client falls
  back to compiled defaults. Documented behaviour, not a fault.
- **The sign-in plan is complete.** `/admin` is linked from `/support`, `SessionControl`
  is in `TopBar`, `SiteNav` and `SiteFooter`, and `/how` has its accounts section.

## The one thing still blocking Ajay's own report

`GET /api/auth/me` returns `{"available":false}` in production, so **no sign-in control
renders anywhere** — which is why he still cannot see the login. The code is right and
the omission is deliberate: a control that goes nowhere is worse than none. It needs
`GOOGLE_CLIENT_ID` and `GOOGLE_CLIENT_SECRET`, which are his to create on his own Google
account, alongside the `IDENTITY_SECRET` still outstanding.

---

# Third pass — what the template stripper had been eating

Both importers deleted every `{{template}}` whole. Most are citations and deserve it.
Two kinds are not:

| Template | What it was | What the reader got |
|---|---|---|
| `{{convert\|375\|F\|C}}` | an oven temperature | "Preheat the oven to ." |
| `{{convert\|39\|C}}` | a curd temperature | "over a wood fire to about ." |
| `{{lang\|it\|…}}` | a cheese's name | "generally known as ." |

**An oven temperature is the one number in a step nobody can infer.** A cook can judge
"until golden"; nobody can judge 175°C against 220°C. And every one of these still read
as a sentence, so nothing looked broken enough to check.

87 records repaired: 24 catalogue, 12 cuisines, 51 English recipes.

## Three faults that were discarding whole passages

Each of these threw away prose entirely, so the record showed *no* method rather than a
damaged one — which is why they never looked like bugs:

1. **Templates stripped with a regex.** `/\{\{[^}]*\}\}/` cannot see a template inside a
   template, so it stopped at the first `}}` and left a bare one behind. Prose holding a
   stray brace is then rejected whole. Parmesan had lost a 3,277-character production
   section this way.
2. **Image links matched with a negated class.** A caption containing a link of its own
   ended the match early and left a pipe behind. Same outcome.
3. **`<gallery>` blocks.** Files are listed bare, one per line, no brackets to count. The
   tags came off and the filenames stayed behind as prose.

All three are counted now rather than pattern-matched. The lesson repeats: *a regex
cannot parse a nested structure, and the failure is silent because the guard downstream
looks like it is working.*

## The accident, recorded because it was mine

I checked whether `ingest-wikibooks.mjs` guarded its `main()` by grepping for
`import.meta.url`. It matched — on line 34, an unrelated use. `main()` was **not**
guarded, so importing `section()` started a full cookbook ingest that rewrote
`src/data/cookbook.json` underneath the repair pass reading it. Caught it at 6,980 rows
against an expected 6,979 and restored from git; the enrichment fields were intact.

Two things to keep from that: **grep matching is not proof of the thing you inferred**,
and an import must never have a side effect that large. Both ingest scripts are guarded
now.

## Two false trails, both worth remembering

- **"Rate limiting."** 190 of 256 recipes came back unrepairable, and a diagnostic run
  hit *"You are making too many requests to the API."* I added throttling and backoff.
  The number did not move. The real cause was that the cookbook is **five wikis** —
  `pt.wikibooks.org/Livro de receitas`, `es.wikibooks.org/Artes culinarias` — and the
  repair asked `en.wikibooks.org` for every title. The throttling was still worth
  keeping; it just was not the bug. *A plausible cause that reproduces is still not the
  cause.*
- **"371 damaged strings."** The first count included every field and several gaps per
  string. The real number of damaged summaries was 27.

## Deliberately not fixed, and counted rather than hidden

- **190 recipes** on the it/fr/de/pt/es Wikibooks. They name a method differently
  (Preparazione, Préparation, Zubereitung, Modo de preparo) and the Spanish book has no
  headings at all — its recipes are the parameters of a `{{Datos de receta}}` template.
  They need `ingest-cookbooks-multilingual.mjs`'s extractor, not this one.
- **15 English recipes** whose value is missing on Wikibooks itself.
- **9,219 records with no `prepSummary` at all.** Some fraction were lost to the three
  faults above and would come back from a re-enrichment — that is ~460 API requests and
  a large diff, so it is a decision rather than a cleanup.

## Where the invariant draws its line

Zero for catalogue, cuisines, gi and unesco. A ceiling for the cookbook that can only
fall — if a change repairs more, the test fails and the ceiling comes down with it.

It checks only the unambiguous shape: a space before a comma or full stop. The collapsed
form ("Preheat an oven to.") is what the repair passes hunt for, but it cannot be an
invariant — *"used to rub the bread with."* is a whole sentence, and a test calling that
a defect would be wrong five times in the catalogue alone.

---

# Fourth pass — the other five cookbooks

The previous pass reported **190 recipes as unrepairable**. They were not. The cookbook is
drawn from six Wikibooks — en, it, fr, de, pt, es — and the repair asked
`en.wikibooks.org` for every title. A Portuguese page answers "missing" there, which the
loop counted as a skip, so the number read as a fact about Wikibooks rather than a bug in
the client. **The language was in each row's own URL the whole time.**

211 recipes mended across all six, steps and ingredients alike:

| | before | after |
|---|---|---|
| es | "Machacamos el ajo en un , con la sal" | "…en un **almirez**, con la sal" |
| es | "En una , ponemos aceite" | "En una **olla**, ponemos aceite" |
| en | "Preheat the oven to ." | "Preheat the oven to **375 °F**." |

Each book is read the way it writes a method, through the extractor that first wrote it:
headed sections for en/it/fr/de/pt, and for Spanish the parameters of a
`{{Datos de receta}}` template, because that book has no headings at all.

Three causes turned out to hide behind one symptom, which is why "0 repaired" was the
first result after the language fix:

- **es** — `{{ute|almirez}}` and `{{ing|Agua}}`, the book's own utensil and ingredient
  templates. Real losses, 113 records.
- **fr, it** — stray spaces in the source wikitext (`"le pain plat (Pide) ."`). Nothing
  missing; normalised rather than repaired.
- **en** — `{{convert}}`, already handled.

## Two faults of mine, both caught by tests

1. **Order.** I put the rendering *after* the rule that strips a trailing `}}`, so a
   template at the end of a line arrived unclosed and matched nothing — `{{ing|Agua`
   reached the reader as raw markup. The catalogue's own invariant caught it. *A pipeline
   step that removes structure must run after the steps that read it.*
2. **Accepting markup.** The repair then took lines still holding `{{rec|…}}` and a stray
   `|15px` off an image parameter. A repair that trades a missing word for wikitext is not
   a repair. A re-derived list holding a brace or a pipe is now refused **whole** — the
   same rule the English ingest already applied — and ten recipes are declined on that
   ground, keeping a gap that is already recorded.

Both were found by the suite rather than by me reading output, which is the argument for
having written the invariant first.

## Where it stands

22 wounds across the two cookbook files, every one either damaged on Wikibooks itself or
refused above. Ceilings in `plumbing.test.ts` can only fall.

Still open from the third pass: **9,219 records with no `prepSummary` at all**. Some
fraction were lost to the nested-template and gallery faults and would come back from a
re-enrichment — ~460 API requests and a large diff, so it is Ajay's call rather than a
cleanup.

---

# Fifth pass — the re-enrichment, and the correction it produced

I expected the 9,219 records with no method to be largely records the three stripper
faults had robbed. **They were not.** The pass recovered 36 of 3,506 — 1% — and the
headline is the correction, not the gain.

Measured *before* running, on 120 records sampled evenly rather than off the head of the
file: **119 had no preparation, production or method heading at all.** The single
exception had one, and it is a bullet list — 49 characters once list lines are dropped,
correctly refused by the 120-character floor. There was no fourth fault to find.

5,713 of the 9,219 have no URL to re-read at all: a GI or PAT register entry is a name, a
place and a legal reference, with no article behind it.

36 records did gain a real method — chimole, rødgrød, Brie de Melun, German baked apples —
so the run was worth making. But the estimate that motivated it was wrong, and the reason
it was cheap to discover is that the sample cost 6 requests and the full run cost 176.

**Sample before you commit to the run, and spread the sample.** The first-60 sample gave
1/120 too, but only by luck: these files are ordered, and the head is not typical.

## And nine names nobody could type

"Fave bianche e  cicorie PAT", "Tagliatelle  all'acciaccata PAT" — a doubled space inside
the name. Invisible on screen, because HTML collapses the run. But a name is typed in
order to be found, and an exact-match search for the name as written misses it.

The register ingest's own `tidy` collapses whitespace, so these arrived past it — which is
the whole argument for checking the published file rather than trusting the writer. An
invariant now holds every published file to one space between words.

## A race worth noting

The recovery pass loaded `catalogue.json` at start and wrote it at the end. I fixed the
doubled spaces in that same file while it was running, and the write overwrote my fix. No
harm — the fix is idempotent and was simply re-applied — but two writers on one file is
how a repair silently disappears. Finish one, then start the next.

---

# Sixth pass — the records nobody could type

All that repair work was in service of records being *findable*, so the next thing to
check was whether search could actually reach them. It could not.

Search compared lowercased strings and nothing else. **1,957 records — one name in ten —
were effectively unreachable**: Bagòss, crème brûlée, aşure, Erdäpfelknödel, niño
envuelto, rødgrød.

This is the same failure as the Cyrillic *с* inside "cheese", from the other side. There
the data held a character nobody would type; here the reader was asked to type one they
cannot reach. Both end with a record that exists and cannot be found. **The reader who
knows a dish well enough to look it up is usually the one least able to type its name.**

`src/domain/fold.ts` drops the marks Unicode can decompose and maps by hand the letters it
cannot — ø, ß, æ, œ, ł, đ, ð, þ, ı — because those are letters in their own right rather
than a base plus a mark, and survive `NFD` untouched.

Two rules that matter more than the mechanism:

- **Both sides, never one.** Folding only the query leaves "creme" missing "crème"
  exactly as before.
- **Latin only.** A Devanagari or Arabic mark is not a decoration on a letter, it is part
  of which letter it is. Folding those would make search answer with a *different word*.
  There is a test asserting कढ़ी, كبسة and 刀削麵 come back unchanged.

Verified in the search box on production rather than only in tests:

| typed | result |
|---|---|
| `hakarl` | 2 matches, Hákarl |
| `rodgrod` | 1 match, Rødgrød |
| `creme brulee` | 3 matches |
| `cordoba` | 2 matches |
| `bogoss` | 0 — the fold has not become a wildcard |

## A note on driving the app to check

`computer type` did not land in the search box — react-native-web's controlled input
ignored the synthetic keystrokes and `input.value` stayed empty while the results list
sat unchanged, which looks exactly like "the feature does not work". Driving it through
React's own value setter and dispatching `input` exercises the same `onChange` path and
does land.

Worth remembering before concluding from a browser check that a feature is broken: **the
harness failing to type is indistinguishable from the app failing to search**, unless the
input's value is read back.

---

# Seventh pass — search that answers the question asked

Two faults, found by driving the real search box rather than reading the code.

## Word order was load-bearing

The query was matched as one substring, so the reader had to type a record's words in the
order the record happens to store them. Measured on production:

| typed | before | after |
|---|---|---|
| `pizza margherita` | 2 | 2 |
| `margherita pizza` | **0** | 2 |
| `halwa kozhikode` | **0** | 1 |
| `mole oaxacan` | **0** | 1 (Oaxacan Mole Negro) |
| `pizzza` | 0 | 0 |

The query is now a set of terms, each folded, all of which must appear — so it stays a
narrowing search rather than a fuzzy one, while the terms may fall anywhere, in any order,
and across different fields. "naples pizza" takes the place from the breadcrumb and the
name from the title.

## Relevance was not part of the ordering at all

Searching "tomato" returned 411 records ordered by evidence, and all twelve records
actually *named* for a tomato sat at the bottom, because an unscored record scores zero.
The search was answering "the best records that mention tomato" when it was asked
"records about tomato".

A name match now outranks a mere containment, and *within* each group the reader's chosen
sort applies untouched — so the file's existing rule, that authenticity and popularity
never feed each other, still holds. They were not mixed; they were asked a narrower
question first.

Two tiers only. Finer scoring — how early the word appears, how much of the name it covers
— is guesswork dressed as precision, and this atlas has enough numbers that mean something.

## Two ways a browser check can lie, both hit today

1. **The harness could not type.** `computer type` did not reach react-native-web's
   controlled input: `input.value` stayed empty and the results list sat unchanged, which
   is indistinguishable from a broken search. Reading the value back is what caught it.
   Driving through React's own value setter and dispatching `input` works.
2. **The page was running last week's code.** After deploying, the first check said
   nothing had changed. The browser held a cached `index.html` pointing at
   `entry-4ed4ec5a` while the deployment served `entry-419cf44a`. Comparing the bundle
   name in the page against the one the site serves settled it in one call.

Both produce the same symptom — "the change did nothing" — and neither is a fact about the
change. **Check that the page is running the build you think it is before believing a
negative result.**

---

# Eighth pass — the heading outline, finally verified

The heading roles shipped days ago and were recorded as unverified. Checked in a browser
at last, and two faults were visible immediately.

## Five headings on the home page were not headings

`Stat` used `H6` for its "DISHES", "COUNTRIES", "DOCUMENTED", "REGISTERED", "AUTHENTIC"
labels, purely for the uppercase treatment. Each entered the document outline, so somebody
navigating by heading met five section titles that are not sections — each announcing a
single word with no content beneath it.

H6's own comment already said what it is: *"It is a signpost, not a heading."* And marked
it as one anyway. `Eyebrow` is that signpost — same treatment, no role.

## Levels were inferred from size, and size is not depth

"Most looked up" is a rail on the home page, a sibling of "Disappearing" and
"Authenticated", but it is drawn small — so it announced itself as level 4 straight after
level 2. The record page was shifted the same way throughout: its sections sat at level 3
with nothing at level 2 above them.

Headings now take an optional `level`, so a smaller treatment can keep its real place in
the outline without changing what it looks like. **Where size and depth disagree, the
outline wins.**

| | before | after |
|---|---|---|
| home page headings | 12, five of them phantom | 7, all real |
| home page skips | 2 → 4 | none |
| record page skips | 1 → 3 | none |

## Two more zero-viewport artefacts, correctly ignored

The Browser pane reports `innerWidth: 0`, so `useLayout().wide` is false and the desktop
footer legitimately returns null — `role="contentinfo"` was absent, and it would have been
easy to file that as a missing landmark. It is not: the phone colophon renders in its
place, exactly as designed. Same family as the `requestAnimationFrame` finding earlier:
**a zero-size hidden pane makes several correct behaviours look like faults.** Check the
viewport before believing one.

---

# Ninth pass — the page was 15 MB, and 9 of it was thumbnails

Ajay asked weeks ago why rendering was slow. The first-paint work cut chrome and deferred
the method text. This is the rest of the answer.

The atlas requested **full-resolution originals** and drew them in a 192px card. On the
live home page: **8.9 MB of photographs for 63 thumbnails**, one of them 1,645 KB alone,
the largest arriving at 2269×1856 to be shown at 192×192.

Commons scales on request. Measured over 22 of the home page's photographs:

| | |
|---|---|
| originals | 19.65 MB |
| at `width=384` | **1.07 MB** |
| saving | **95%** |

Every Wikimedia URL is now normalised to `Special:FilePath/NAME?width=N`, with the width
coming from the call site: a shelf card asks for twice its own card size, the record hero
for 1200, everything else 400.

## The repair that would have broken 5,998 photographs

Three URL shapes, none width-limited: 3,055 `Special:FilePath`, 5,998
`upload.../thumb/...`, 1,585 originals. The obvious fix for the middle group is to edit
the `960px-` in the path down to `384px-`.

**Commons answers HTTP 400 for that.** Every one of those 5,998 would have become a broken
image — and a test that only checked the URL had changed would have passed. Four curl
calls before writing any code is the only reason that is not in the commit.

The file name is recoverable from all three shapes, so all three normalise to the one form
that works rather than each being patched its own way.

## Two smaller honesty notes

- For an original already narrower than the requested width, the re-encode can come back
  *larger* — 2 of the 22 measured. A rounding error against 95%, but it is real and it is
  in the commit message rather than left out of it.
- `encodedBodySize` reads 0 for these now: the request redirects cross-origin and the
  timing data is withheld. The browser could no longer measure what it measured before the
  change, so the after-figure was taken with curl. **When the instrument stops reporting,
  that is not the same as the number being zero.**

---

# Tenth pass — the render was waiting on files nobody had asked for

`app/_layout.tsx` holds the whole app behind its fonts:

```tsx
if (!fontsLoaded) return <FeedSkeleton fonts={false} />;
```

And the five faces are referenced from inside the JavaScript bundle, so the browser could
not learn they existed until that bundle had downloaded, parsed and run.

| | before | after |
|---|---|---|
| JSON (already preloaded) | starts 77 ms | — |
| JS bundle | 77–884 ms | 218–1,126 ms |
| **fonts** | **starts 1,211 ms, done 1,424 ms** | **starts 218 ms, done 725 ms** |

667 KB of font sat unrequested for a second and a quarter while the reader watched a
skeleton. The preload script covered the five JSON files — which the render does *not*
block on — and not the fonts, which it does. **Preload what the render waits for, not what
is merely large.**

The fonts now finish four hundred milliseconds *before* the bundle they used to wait for,
so they have stopped being the constraint at all.

Two details worth keeping:

- The list is discovered from the build output, not written in the script. The names carry
  a content hash that changes whenever a face does, and a hard-coded list would eventually
  preload a file that no longer exists — worse than no preload, because it spends a request
  and warms nothing.
- `crossorigin` on every font link, for a different reason than the JSON links need it: a
  font is always fetched anonymously, so a preload without it is a second unmatched request
  rather than a head start. That failure is silent, so it was checked in the browser
  afterwards — every font and every source appears exactly once in the resource timing.

## Still on the table

- The JS bundle is **1,720 KB on the wire, 8.5 MB decoded**, and is now the longest pole
  in the load. Splitting an Expo Router bundle is a real piece of work rather than a tweak.
- The fonts are TTF at ~335 KB each. WOFF2 would be roughly half, but `@expo-google-fonts`
  ships TTF and converting needs tooling this environment does not have.
- `/api/auth/me` is requested twice on every load. Harmless, but it is two requests where
  one would do.

---

# Eleventh pass — what the bundle is made of, and where I stopped

## Measured, then chose not to act

The JS bundle is the longest pole in the load now: **1,278 KB brotli, 8.3 MB decoded**.
Broken down, the largest single thing in it is ours:

| | |
|---|---|
| `src/i18n/catalogues.ts` raw | 895 KB — 45% of all our source |
| the same, brotli | 194 KB |
| share of the wire bundle | **~15%** |

Every reader downloads twelve languages in order to read in one.

Splitting the eleven non-English catalogues out would save roughly 178 KB on the wire —
about 14% of the bundle — plus the parse cost, which matters more on a phone than the
transfer does. That is a real win, and it is **not** in this session, for reasons worth
writing down:

- `copyFor()` is synchronous and is called by every screen through `useCopy()`.
- `UI_LOCALES` is derived from `Object.keys(CATALOGUES)`, so the locale list would have to
  become a static list.
- The language picker shows a **coverage percentage for every locale**, computed from the
  catalogues themselves. Lazy-loading the strings means precomputing those twelve numbers
  at build time, or the picker silently reports the wrong figures — which in this project
  is worse than being slow.

Three moving parts in the core that every screen reads through, against ~150–250 ms. It is
the right next piece of work and it is a piece of work, not a tweak. Flagged for Ajay
rather than started at the end of a long session.

## What was done instead, bounded and verified

`/api/auth/me` was fetched twice on every load: `SessionControl` renders in three places
and `ConfirmForm` asks as well. The in-flight promise is shared now, and never invalidated
— signing in and out are full-page navigations, so the only two ways the answer can change
both discard the module along with the page. **A cache that cannot go stale needs no way
to be cleared.**

Verified: one request, and nothing on the page fetched twice.

## A sweep that found nothing, which is also a result

All nine routes — atlas, browse, search, how, support, proposals, contribute, propose,
dish — render with content and **zero console errors, zero unhandled rejections**. Worth
the two minutes it cost to know.

---

# Twelfth pass — twelve languages for a reader who wants one

`src/i18n/catalogues.ts` was **895 KB of source, 45% of everything in `src/` and `app/`**,
and ~15% of the bundle on the wire. Every reader downloaded all twelve languages to read
in one.

The eleven non-English catalogues are generated to `public/data/copy/<locale>.json` at
build time and fetched per reader. English stays compiled in — it is the fallback behind
every key and has to exist before anything renders.

| | before | after |
|---|---|---|
| bundle raw | 8.31 MB | 7.20 MB |
| bundle brotli | 1,278 KB | **1,082 KB** |
| English reader's extra fetches | — | **0** |
| Japanese reader | — | one 24 KB file, inside the existing data wait |

## The part that needed care

Two things were derived from having all twelve in memory:

- `UI_LOCALES`, from `Object.keys(CATALOGUES)`
- `translationCoverage()`, the figure the language picker prints as "97%"

The second is the dangerous one. Counting what happens to be *loaded* would report **0%
for every language on offer** — a confident wrong number rather than a slow one, which is
the trade this project refuses everywhere else. Both are computed from the real catalogues
at build time into `src/i18n/manifest.ts`, and **a test asserts the manifest still matches
`catalogues.ts`**, so editing a translation without re-running the generator fails the
suite instead of drifting into a picker nobody measured.

## Two decisions inside the change

- **`setLocale` waits for the words before switching.** Switching first and swapping the
  strings in afterwards would show a reader a page of English on the way to the language
  they just asked for — a flash of the one thing they were trying to get away from.
- **Tests install the catalogues through the same seam the fetch uses.** Without
  `__tests__/setup.ts`, every test checking a translation would have been handed the
  English fallback and passed for the wrong reason — worse than failing, because it looks
  like coverage.

## Verified on the deployment, not only in tests

English: 0 copy fetches. Japanese: one 24 KB fetch at 441–481 ms, page in Japanese.
Switching: clicking *Français* fetched `fr.json` only, the page turned French, the choice
persisted. The bundle contains all twelve coverage numbers and **no catalogue strings**.

A note on that last check: the numbers appeared "missing" from the bundle at first, because
the minifier writes `0.98713` as `.98713`. The grep was wrong, not the build — **check the
form your evidence takes before believing what it fails to find.**

---

# Thirteenth pass — shipping the prefix in order to throw it away

Every one of the 10,638 photographs sits behind one of three Wikimedia URL prefixes,
repeated on every record — **1.8 MB of prefix** across the published files. Since the app
now rebuilds the URL at render time (to ask for the width it will draw at), the prefix was
being shipped to be discarded.

Two more fields went with it:

| field | why it went |
|---|---|
| `langs` | identical to `Object.keys(langNames)` on **all 3,933 rows** — derived at build instead |
| `placeConfirmed` | declared on the row type, read by nothing |

| | before | after |
|---|---|---|
| published payload | 14.7 MB | 13.0 MB |
| JSON a phone parses | 16.0 MB | **14.0 MB** |

## Measure first, because the obvious assumption was wrong

The instinct is that removing 1.8 MB of repeated text saves a lot of transfer. It does
not: brotli compresses a repeated prefix to nearly nothing. Measured before writing any
code — **5% on the wire, 17% of the JSON that must be parsed**. The win is CPU on a
low-end phone, not bytes on the network, and it is worth having for that reason and not
the other one. Had I assumed, I would have claimed the wrong benefit in the commit
message.

## One photograph in 10,638 keeps its address

An Italian Wikibooks recipe is illustrated with a file uploaded to `it.wikibooks`, not to
Commons. `Special:FilePath` on commons.wikimedia.org answers 404 for it, so keeping the
full URL is correct.

My first test asserted that *no* full URLs remained. **That test was wrong, not the data.**
A rule like that pushes the code into breaking a working photograph for the sake of
consistency. It now asserts the narrower true thing: no *Commons* URL may survive
uncompacted, and every stored value must resolve through `sizedPhoto` to either a Commons
address or itself unchanged.

Worth remembering as a pattern — **when a sweeping invariant fails on one row, check
whether the row is special before making the code obey the rule.**

Verified on the deployment: 63 photographs on the home page, none broken.

---

# Fourteenth pass — four investigations, three of which found nothing

Written down because "nothing was wrong" is a result, and because two of these were false
alarms of my own making.

## Deferring the recipe steps further: not worth it

`cookbook-detail.json` is 3.92 MB and is fetched on every page load, including the home
page where no step is ever shown. The plan was to move it to idle time so its parse does
not land in the first-paint window.

**Measured first: the parse is 11 ms.** The walk over 6,979 records is 0 ms. On a phone
call it 40–60 ms. There is nothing there worth moving, and the change would have traded a
warm record screen for a saving that does not exist. Not done.

What it does cost is ~1.2 MB of transfer for a reader who never opens a recipe — a real
trade-off in both directions (defer it and every recipe opens ~300 ms slower), so it is a
decision to put to Ajay rather than a fault to fix.

## "888 duplicate records": mine, twice over

A first sweep reported **894 duplicate name+country groups**, including one Greek aubergine
appearing **115 times**. Both numbers were wrong.

1. My normaliser stripped everything outside `[a-z0-9 ]`, which deletes **all** Greek,
   Chinese and Thai characters — so 115 *different* Greek records collapsed onto one empty
   key. Fixed to fold Latin marks only and keep letters of every script.
2. The corrected sweep still showed 888 groups spanning `catalogue`, `cuisines` and
   `cookbook` — but **the build already merges them**. 21,688 source rows become 17,740
   traditions, and searching "tzatziki" returns 2 sensible results, not 5.

So: no defect. A fact about the source files, not about what a reader sees. The same
lesson as "presence is not proportion", in a new costume — **normalise without destroying
the thing you are counting.**

## Mobile, finally verified at a real 375px

The Browser pane reports a usable viewport now, so Ajay's original complaint could at last
be checked rather than reasoned about:

| | |
|---|---|
| page height | 4.9 screens |
| first photograph | 359 px — 0.44 screens |
| tap targets under 40 px | **0** |
| page scrolls sideways | **no** (`documentScrollWidth` 375 = viewport) |
| horizontal rails | 7, every one `overflow-x: auto` and contained |

The elements wider than the viewport are the shelves, which are meant to be.

## Scroll reveal: still not verifiable here, still recorded as such

`visibilityState` is `hidden` and `requestAnimationFrame` returns **0 frames in 500 ms**,
in a fronted pane at a real viewport size. The five shelves read `in` at 41 seconds, which
is the 3-second failsafe having fired — the designed degradation, not evidence about the
scroll behaviour. Unverified is still the honest word for it.

---

# Fifteenth pass — the atlas could not be shared

An area nothing had looked at. The served page carried **three meta tags** — charset,
X-UA-Compatible, viewport — and nothing else.

- No `description`
- No Open Graph
- No Twitter card

So every link pasted into WhatsApp, Slack, Signal, Discord or a group chat rendered as a
bare URL: no title, no summary, no picture. **For a free atlas with no advertising, word
of mouth is the whole distribution strategy**, and a link that looks like nothing does not
get opened.

## robots.txt and sitemap.xml both returned 200 without existing

They were never written. The single-page app's catch-all handed crawlers `index.html` and
a 200 status, so a check of the status code alone said "fine".

This is the third time this exact shape has appeared in this project — the fonts that
404'd into index.html, and now these. **A 200 is not the same as an answer.** Both are
real files now; Cloudflare Pages serves a real file ahead of the catch-all.

## The card picture is the app's own mark, deliberately

The obvious social card is a photograph, and there are 10,638 of them. Nearly all are
CC BY-SA, which requires attribution *wherever the work is shown*, and a link preview has
nowhere to put a credit line. `SiteFooter` already documents why there is no copyright
line on this site — the atlas does not own this material. An unattributed photograph on
every share would be the same mistake pointing the other way.

## Two things left undone on purpose

- **Per-record cards.** One `index.html`, routes resolved on the client — there is no
  per-path HTML to write a per-path tag into. `/dish/1` and `/atlas` share these tags.
  Doing it properly means prerendering: a piece of work, not a build step.
- **The sitemap lists six screens, not 17,740 records.** Every record would offer a
  crawler the same title and description. Listing them is asking to be judged as thin
  content rather than helping anybody find a dish.

Verified on production: twelve tags served, both files return their own contents, card
image 200 at 13 KB.

---

# Sixteenth pass — route splitting does not exist here, and two missing headers

## The negative result first

`expo.experiments.asyncRoutes` was the obvious next lever on a 1,082 KB bundle. Enabled it
for web, rebuilt, and got **one bundle of exactly the same size**. Expo SDK 57's static web
export ignores the flag. Reverted rather than leave a setting that does nothing but might
change behaviour later.

Route-level splitting is therefore not a config change in this stack. It stays on the
pending list as real work.

## Content-Security-Policy, generated rather than typed

`public/_headers` already carried nosniff, frame options, referrer policy and a permissions
policy, each with its reasoning. CSP could not be written by hand.

A useful `script-src` names what may run, and this site has exactly one inline script — the
canonical-link injector. Allowing it means either `'unsafe-inline'`, which permits *any*
injected script and gives up most of what a CSP is for, or its SHA-256. **A hand-written
hash stops matching the next time that script changes**, and the canonical tag quietly
stops running — a failure that surfaces in search results weeks later, not in the build.
So it is computed from the file that shipped, on every build.

Two permissions that are costs rather than oversights, and are written down as such:

| | why |
|---|---|
| `style-src 'unsafe-inline'` | react-native-web builds its stylesheet at runtime through the CSSOM — no file to name, no stable hash to pin |
| two Wikimedia hosts in `img-src` | Commons serves the photograph and redirects to `upload.wikimedia.org`; one image needs both |

`connect-src` is `'self'` only — API, catalogue and per-locale chrome all come from this
origin.

**HSTS** deliberately without `includeSubDomains`: ajailabs.app carries other projects and
this deployment has no business making a promise on their behalf.

## Verified, because a wrong CSP fails silently

Nine routes walked and a record page loaded cold: **zero `securitypolicyviolation`
events**, 64 photographs none broken, three YouTube thumbnails intact, five fonts, six data
files — and the canonical tag present, which is the proof that the hashed inline script was
allowed to run. That last check is the one that matters: it is the only way to tell an
allowed script from a blocked one without reading the console.

---

# Seventeenth pass — the spend limit was a race

`/api/translate` is the only endpoint that can cost money, and its ceiling did not hold
under load. The order was **read `spent` → call the model → increment**. On a Worker that
is a race with real concurrency behind it: every request arriving while a call is in
flight reads the same figure and every one decides there is room.

The overshoot is bounded by how many arrive together — which is exactly the situation a
limit exists to rule out. **A burst is what a script looks like.**

Check and increment are now one statement:

```sql
ON CONFLICT(day) DO UPDATE SET spent = spent + 1 WHERE spent < ?
```

A failed model call hands the place back, which is what the old order got right and this
keeps: a reader who received nothing should not be charged.

## Two unchecked inputs

`target` becomes `translation.lang`; `dish` becomes `translation.dish_id`. Any string and
any integer could be written — a day of rows filed under languages that do not exist,
against records that do not exist. The meter bounded the *cost*, which is why it was never
dangerous, but a cache keyed on nonsense never hits and grows for ever. Neither new check
refuses anything the app sends.

## Verified against production, both branches

| | |
|---|---|
| bad language tag | 400, meter unchanged |
| impossible record id | 400, meter unchanged |
| cached pair | served free, meter unchanged |
| first real call | created the day's row at 1 — the **INSERT** branch |
| second real call | 1 → 2 — the **conditional UPDATE** branch |

Only the second branch is the ceiling, so testing one would have proved the wrong half.

## I nearly reported my own fix as broken

After the first real call the meter still read 8 and I had the fix half-condemned. UTC had
rolled to the 29th and the counter is keyed on UTC, so the increment had gone to a new
row — my shell was showing the local date. **When a counter looks stuck, check which
bucket you are reading before concluding the writer is broken.**

## Still open on this endpoint, and worth Ajay knowing

The `prompt` is accepted verbatim, so this is effectively an open text-generation endpoint
for anyone who finds it, bounded at 500 calls a day. It cannot run up an unbounded bill —
that is what the meter is for — but a stranger could spend a day's translations on
something that is not translation. Closing it properly means the server building the
prompt from a record id, which it cannot do while the catalogue lives in files rather than
in D1. Recorded rather than half-solved.

---

# Eighteenth pass — the second endpoint that spends, and a file that reviewed as binary

## The same race, where it mattered more

`/api/polish` shares one counter with `/api/translate`, deliberately: a second AI feature
should raise how *fast* the day's budget is consumed, never how much can be spent. It
shared the race too — read `spent`, call the model, increment afterwards.

**Two racy readers of a single ceiling can overshoot it together**, which defeats the
reason for sharing the counter in the first place. Both endpoints now reserve and check in
one statement, and both refund when the model call fails.

## A source file that greps as binary gets skipped

`functions/api/events.ts` held a **literal NUL byte**. The choice is sound — a separator in
a Map key that cannot appear in either half — but typing it raw rather than writing
`\u0000` makes the entire file read as binary to grep, ripgrep and editor search.

It nearly skipped this review. The first sweep across the endpoints returned
`Binary file functions/api/events.ts matches` and I almost moved on. Same behaviour now,
written as an escape, with a note saying why.

**A file your tools cannot read is a file nobody reviews.**

## Reviewed and found sound, so left alone

| | |
|---|---|
| `_admin.ts` | compares the token in constant time |
| `analytics`, `refresh`, `settings` | gate on admin before reading anything |
| `events` | caps a batch at 40 |
| every query in `functions/` | parameterised |

## Verified on production

Polish returned a corrected sentence and charged the shared meter exactly once (2 → 3),
fixing spelling and apostrophes while leaving "coconut oil not ghee" untouched — the
content rule that endpoint exists to keep. Events accepted a batch and returned 200.

---

# Nineteenth pass — a session that never actually expired

Sign-in is not configured yet, which makes this the right moment to read the OAuth code
rather than the wrong one.

## The flaw

The session cookie carried `account.signature` and nothing else. `Max-Age` did all the
expiring — and **`Max-Age` is an instruction to a browser, not a fact the server can rely
on.** Anything that kept sending the cookie stayed signed in *for ever*: a copy taken off
a shared machine, a value pasted into curl. The signature never went stale because there
was nothing in it that could.

`_session.ts` says thirty days is "short enough that a shared computer forgets". That was
only true of a browser that chose to honour it.

The moment of issue is signed alongside the account now and checked on the way in.
`newSessionCookie` creates the stamp and the signature together, so there is no way to
produce one without the other.

## What was already right, and left alone

| | |
|---|---|
| `state` | random UUID, HttpOnly + Secure + SameSite=Lax, scoped to `/api/auth`, compared on return and cleared to stop a replay |
| account id | salted one-way hash of the provider's subject — no email, no profile, no user table |
| signing | HMAC-SHA256, constant-time comparison |

## Tested, because it cannot be clicked

The sign-in routes cannot be exercised through the app until the Google credentials exist,
so `__tests__/session.test.ts` runs against the real functions — 11 cases.

The one that matters: a cookie stamped **thirty-one days ago is refused while its signature
is demonstrably valid**. The test asserts the signature is good *before* asserting the
cookie is rejected, so it cannot pass for the wrong reason. Also refused: a future stamp, a
non-numeric stamp, a moved stamp, a swapped account, a foreign secret, and the old
two-part shape.

**Security code that cannot be clicked has to be tested, or it ships on reasoning alone.**

---

# Twentieth pass — the line the badge rests on, and whether anything would notice

`0001_proposals.sql` already said it, and it was true:

> "This index is what the number '3 confirmations' actually means… **If it is ever
> dropped, the badge stops meaning anything and no test in the app would notice.**"

`assess.ts` counts rows. It cannot tell three people from one person three times, because
by the time it counts, the distinction has already been made — or not made — in SQLite.
Every test of the scoring model would go on passing with the index deleted.

Five assertions over the migrations now cover the constraints the model rests on: one
confirmation per person, one per account, no duplicate proposal, the trigger stopping a
submitter confirming their own dish, and `verified` defaulting to 0 so an unstated
confirmation is the unverified kind.

Reading the migrations as text is weaker than exercising a database, and it is the right
guarantee here: the failure being guarded against is somebody removing or loosening a line,
and that is visible in the file.

## The tests were checked against a broken schema

**A test that cannot fail is worse than no test**, so each was run against deliberately
mutated SQL. All six caught, including the two that are not obvious:

| mutation | caught |
|---|---|
| drop the one-per-person index | ✓ |
| make it non-unique | ✓ |
| widen its predicate past `'published'` | ✓ |
| drop the account index | ✓ |
| drop the trigger | ✓ |
| default `verified` to 1 | ✓ |

The live database was queried separately and carries all three indexes.

## Reviewed and found sound

- `verified` is set from the server's view of the request, never from the body.
- Confirmation text is bounded by a `text(value, max)` helper on every field.
- Both unique indexes are partial for reasons that are written down: a withdrawn
  confirmation must not bar a person for ever, and the unlimited anonymous rows must not
  collide with one another.

---

# Twenty-first pass — acting on the recommendations

## The recipe steps now wait for idle

`cookbook-detail.json` is 3.92 MB — a quarter of everything downloaded — and it started the
instant the build was queued, competing with sixty photographs and five fonts to fill in
words most visits never reach.

**Idle rather than removed.** Dropping the prefetch saves 1.2 MB for every visit that never
opens a recipe and costs ~300 ms to every visit that does, at the moment somebody asked to
read a method. Waiting keeps both.

| | |
|---|---|
| catalogue.json | 94 – 238 ms |
| cookbook.json | 95 – 341 ms |
| cookbook-detail.json | **3,140 – 3,472 ms** (was in the opening burst) |

And the case it could have broken — opening a recipe *before* idle fires: the record
screen's own call runs on mount, fetched at 1,714 ms, method rendered in full.

## The unmatched route was Expo's developer screen

    Unmatched Route
    Page could not be found.
    https://wikifoodia.ajailabs.app/not-a-real-page
    Go back • Sitemap

English whatever language the reader chose, reciting the URL they already know is broken,
and offering **Sitemap** — a development tool — to somebody who mistyped a link.

Replaced in the app's own voice and translated into all twelve. A screen about being lost
is a poor place to start speaking a language the reader did not choose. The path is
deliberately not shown: somebody who followed a stale link did not type it.

## The recommendation I did not take

Per-IP rate limiting on `/api/translate` would close the open-prompt hole properly. It also
means reading the IP — and `events.ts` states the privacy guarantee **structurally**:
*"Nothing identifying is read. Not the IP, not the user agent, not a cookie."* The front
page promises "nothing tracked" in four places.

The global cap already bounds the thing that matters, a bill. The residual risk is a
stranger consuming a day's free translations — a denial of a free feature, not a cost or a
breach. **Not worth trading a public promise for.** Left, and said out loud rather than
quietly built.

## Nearly a second fix that was not needed

`/dish/999999999` looked blank at six seconds. That was the loading skeleton; at eight it
reads "NO RECORD — Nothing in the atlas is recorded under that reference." Measured too
early, not broken. Third time this session that waiting longer was the whole fix.

---

# Twenty-second pass — the last English typed into a screen

Ajay's original report was "some sections in native language while English is selected and
vice versa". Most of it was the translation pipeline. Three labels were something simpler:
English typed straight into JSX, never passing through the copy system.

The worst had a translated sibling one line below it:

```tsx
<T>Traditional: </T>                    // always English
<T>{copy.commonModernSubstitute}</T>    // "Substitut moderne courant : "
```

A French reader saw one label in their language and the next in English, in the same block.
The other two were the "Ingredients" and "Method" headings on the proposals screen.

All three are copy keys now, in twelve languages — including the French spacing rule the
neighbouring key already observed: **"Traditionnel : "** with the space before the colon.

## A test over all 64 screens, not over these three

Two exemptions, both listed rather than assumed:

- **`admin.tsx`** — one person's console, reached by a link only he has. Translating an
  operations screen into twelve languages serves nobody.
- **Example placeholders** — "Kaipola", "Kaipola.jpg", a breadcrumb of place names. Proper
  nouns read the same in every language, and a rule demanding they be translated would be
  wrong.

Checked that it fails: reintroducing the "Ingredients" literal turns that one file red;
removing it turns it green.

## Two small things worth keeping

**The coverage test earned its keep on its first real opportunity.** The full run after
adding the keys failed because `manifest.ts` still held the old numbers — the generator had
not been re-run. That is precisely the drift it was written to catch.

**localStorage is per-origin, and each deploy preview is a new origin.** Setting the locale
on one preview URL and then navigating to the next one leaves the page in English — which
looks exactly like a translation that did not work. Third variety of false negative from
the browser this session, after the cached bundle and the untyped input.

---

# Twenty-third pass — a keyboard could not get past the masthead

96 focusable controls on the home page, **all correctly named** — the keyboard story was
half right. The missing half: eight masthead controls before a single record, on every
page, with no way past them.

WCAG 2.4.1 (Bypass Blocks) is Level A, and this was the app failing it. The heading and
landmark work from the eighth pass helps a screen reader jump around; **it does nothing for
a keyboard, which has no notion of a heading.**

A real `<a href="#main">`, so the browser moves focus its own way with no JavaScript to
fail. `role="main"` gained `id` and `tabIndex={-1}` — a destination, not a stop. Without
the tabindex the anchor appears to work while leaving the reader where they were, because
an element that cannot hold focus silently refuses it.

Hidden by **clipping**, never `display: none` or `visibility: hidden` — both remove an
element from the tab order, which would make this link unreachable by precisely the people
it is for.

## The first placement was useless, and measurably so

Inside `Screen` sounded right. `TopBar` renders in `_layout.tsx` *above* the Stack, so
everything a screen renders comes after it in the document — the link measured as the
**ninth** focusable element. A reader had to tab through all eight masthead controls to
reach the thing offering to skip them. Now index **0**.

Worth the measurement rather than the assumption: "first in the component I edited" and
"first in the document" are different claims.

## A fourth kind of false negative from this pane

The appearance on focus cannot be checked here. `document.hasFocus()` is `false`, so
`:focus` never matches and the link stays clipped however it is focused — the same limit
that has kept scroll reveal unverified all session.

Checked instead, from the CSSOM: the rule exists and its declarations resolve —
`background: rgb(217, 164, 65)` on `color: rgb(22, 24, 38)`, accent on page ground — the
anchor is first, its href is `#main`, and `#main` carries `tabindex="-1"` and `role="main"`.
**The mechanism is verified; the appearance is reasoned from a rule I can read but cannot
watch apply**, and that distinction belongs in the commit rather than in a claim of
"verified".

---

# Twenty-fourth pass — the document said English while the page said Japanese

Expo emits `<html lang="en">` and nothing changed it. A reader who chose Japanese got a
page of Japanese inside a document still declaring itself English.

That attribute is not decoration:

- a screen reader takes its **voice and pronunciation rules** from it — Japanese read with
  English phonetics is closer to noise than to a translation, and the reader has no way to
  know why
- braille output picks its table the same way
- so does the browser's own offer to translate the page

WCAG 3.1.1 (Language of Page) is Level A, and this was the whole of it.

Subscribed to the locale rather than set inside `setLocale`, so it also covers the language
chosen *before the first render* — the case that matters most, because it is every
returning reader who ever picked one.

| | |
|---|---|
| loaded in English | `<html lang="en">` |
| loaded in Japanese | `<html lang="ja">`, page in Japanese |
| switched live to French | `ja` → `fr`, no reload, page in French |

## Checked and already right

All seven form controls on `/propose` carry labels. Recorded because unlabelled inputs are
the most common Level A failure in a form, and this app has three forms — "checked and
fine" is worth writing down when the alternative is checking it again next month.

## The accessibility work, now complete enough to summarise

| | |
|---|---|
| headings | one level-1 per page, no skipped levels, five phantom headings removed |
| landmarks | banner, main, contentinfo, navigation |
| contrast | zero failures on home and record |
| alt text | all 63 photographs |
| control names | 96 of 96 |
| form labels | 7 of 7 |
| bypass blocks | skip link, first in the document |
| language of page | follows the reader's choice |

What remains unverifiable here is the same short list all session: anything needing
`requestAnimationFrame` or real document focus, because this browser pane has neither.

---

# Twenty-fifth pass — a screen that throws went black

There was no error boundary. A render error unmounted the tree and left a black page: no
wordmark, no message, no way back — **indistinguishable from a site that has gone down, on
a site that has not.**

The app already refuses that trade one layer down. `_layout.tsx`, on a failed catalogue
fetch: *"Said plainly rather than left as an empty atlas. A reader who sees no traditions
should be told the data did not arrive, not left to conclude there are none."* This is the
same argument for the case where the fault is ours rather than the network's.

Exported from the root layout so it wraps every route — the rule the masthead and footer
already follow.

**It does not show the error.** A stack trace is for whoever is fixing it; printing it to a
reader is noise that also describes the inside of the app to anybody who asks. One line
goes to the console instead, because a fault nobody can see is a fault nobody fixes.
Nothing is sent anywhere: this app promises no tracking, and **an error reporter is a
tracker with a better reason.**

## Proved on a preview branch, never on production

A unit test cannot show this working — catching is expo-router's job, and mine is only what
appears afterwards. So a deliberately-throwing route went to `verify-error-boundary`, a
preview alias the production domain does not point at.

| | |
|---|---|
| the page | not blank |
| what it said | "THIS SCREEN STOPPED", the body, both ways back |
| what it did not say | no message, no stack, nothing about the app's insides |
| the console | `[screen] deliberate: proving the error boundary renders …` |

The route was deleted before the production deploy, and `/boom` on the live site now shows
the not-found screen.

**A preview branch is the way to test something you must never ship.** It costs one extra
deploy and removes the temptation to reason about it instead.

## One more 200 that is not an answer

`curl /boom` returns **200** on the live site, because the SPA catch-all serves index.html
for every path — the same shape that hid the missing fonts and the missing robots.txt. Here
it is correct behaviour, and the status still tells you nothing. What settles it is what
renders: "NOT A PAGE HERE".
